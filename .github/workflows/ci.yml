name: CI - Docker Build & Test

on:
  push:
    branches: [main, master, docker-setup]
  pull_request:
    branches: [main, master, docker-setup]

jobs:
  docker-build-and-test:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/calc-tests:${{ github.sha }}
            ghcr.io/${{ github.repository_owner }}/calc-tests:latest

      - name: Run tests in Docker container
        run: |
          docker run --rm \
            ghcr.io/${{ github.repository_owner }}/calc-tests:${{ github.sha }}
